# @spacesky-cell/ato-shared

Typed Python discovery, managed-runtime provisioning, and bridge protocol client shared by the ATO CLI and MCP adapters.

End users should install `@spacesky-cell/agent-team-orchestrator`, whose root tarball provides the versioned Python wheel and manifest. This package alone does not contain the wheel.
